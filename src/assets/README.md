# Assets

`src/assets/` now holds project-owned media for the live React portfolio.

- `icons/skills/` contains the local SVG icons used by the orbit canvas.
- `files/` and `img/` keep retained personal media and documents.
- The old template vendor bundle has been removed so the folder stays focused on assets the current app actually owns.
